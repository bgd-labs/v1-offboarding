# build and cache
cache/
out/

# general
.env

# editors
.idea
.vscode

# well, looks strange to ignore package-lock, but we have only pretter and it's temproray
package-lock.json
node_modules

# ignore foundry deploy artifacts
broadcast/

# download folder for local diffing
etherscan

# MAC
.DS_Store